# makefile for GNU make (automatically generated by makemake)
# NOTE: indent with TAB in GNU makefiles!

# paths are relative to the project directory (current directory during make)

EXE    = bin/gcc-classic-stable/AmiTwitter
OBJDIR = o/gcc-classic-stable/

# target 'all' (default target)

all : $(EXE)

$(OBJDIR)amitwitter.o : amitwitter.c
	gcc $(shell gccprefs) -c -o $(OBJDIR)amitwitter.o amitwitter.c

OBJS = $(OBJDIR)amitwitter.o

$(EXE) : $(OBJS)
	gcc $(OBJS) $(shell gccprefs) -o $(EXE)

# target 'strip'

strip:
	strip --remove-section=.comment $(EXE)

# target 'clean'

clean:
	-delete $(EXE)
	-delete $(OBJDIR)\*.o
